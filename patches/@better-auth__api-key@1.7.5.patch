diff --git a/dist/index-BeAW0fNP.d.mts b/dist/index-BeAW0fNP.d.mts
index e644e5f..668c491 100644
--- a/dist/index-BeAW0fNP.d.mts
+++ b/dist/index-BeAW0fNP.d.mts
@@ -428,6 +428,7 @@ declare function apiKey(_configurations?: (ApiKeyConfigurationOptions & ApiKeyOp
       requestCount: number;
       remaining: number | null;
       lastRequest: Date | null;
+      rateLimitWindowStart: Date | null;
       expiresAt: Date | null;
       createdAt: Date;
       updatedAt: Date;
@@ -663,6 +664,7 @@ declare function apiKey(_configurations?: (ApiKeyConfigurationOptions & ApiKeyOp
       requestCount: number;
       remaining: number | null;
       lastRequest: Date | null;
+      rateLimitWindowStart: Date | null;
       expiresAt: Date | null;
       createdAt: Date;
       updatedAt: Date;
@@ -841,6 +843,7 @@ declare function apiKey(_configurations?: (ApiKeyConfigurationOptions & ApiKeyOp
       requestCount: number;
       remaining: number | null;
       lastRequest: Date | null;
+      rateLimitWindowStart: Date | null;
       expiresAt: Date | null;
       createdAt: Date;
       updatedAt: Date;
@@ -1148,6 +1151,7 @@ declare function apiKey(_configurations?: (ApiKeyConfigurationOptions & ApiKeyOp
         requestCount: number;
         remaining: number | null;
         lastRequest: Date | null;
+        rateLimitWindowStart: Date | null;
         expiresAt: Date | null;
         createdAt: Date;
         updatedAt: Date;
diff --git a/dist/index.mjs b/dist/index.mjs
index bf85c8b..bf92c0c 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -372,20 +372,27 @@ function applySortingAndPagination(apiKeys, sortBy, sortDirection, limit, offset
 async function listApiKeys$1(ctx, referenceId, opts, paginationOpts) {
 	const storage = getStorageInstance(ctx, opts);
 	const { limit, offset, sortBy, sortDirection } = paginationOpts || {};
+	// The public route filters and paginates after this query. An omitted limit
+	// must read every row, not inherit the adapter's default findMany limit.
+	const readKeys = async () => {
+		const keys = [];
+		const batchSize = limit ?? 100;
+		let nextOffset = offset ?? 0;
+		while (true) {
+			const page = await ctx.context.adapter.findMany({
+				model: "apikey",
+				where: [{ field: "referenceId", value: referenceId }],
+				limit: batchSize,
+				offset: nextOffset,
+				sortBy: { field: sortBy || "id", direction: sortDirection || "asc" }
+			});
+			keys.push(...page);
+			if (limit !== void 0 || page.length < batchSize) return keys;
+			nextOffset += page.length;
+		}
+	};
 	if (opts.storage === "database") {
-		const [apiKeys, total] = await Promise.all([ctx.context.adapter.findMany({
-			model: "apikey",
-			where: [{
-				field: "referenceId",
-				value: referenceId
-			}],
-			limit,
-			offset,
-			sortBy: sortBy ? {
-				field: sortBy,
-				direction: sortDirection || "asc"
-			} : void 0
-		}), ctx.context.adapter.count({
+		const [apiKeys, total] = await Promise.all([readKeys(), ctx.context.adapter.count({
 			model: "apikey",
 			where: [{
 				field: "referenceId",
@@ -416,19 +423,7 @@ async function listApiKeys$1(ctx, referenceId, opts, paginationOpts) {
 				};
 			}
 		}
-		const [dbKeys, total] = await Promise.all([ctx.context.adapter.findMany({
-			model: "apikey",
-			where: [{
-				field: "referenceId",
-				value: referenceId
-			}],
-			limit,
-			offset,
-			sortBy: sortBy ? {
-				field: sortBy,
-				direction: sortDirection || "asc"
-			} : void 0
-		}), ctx.context.adapter.count({
+		const [dbKeys, total] = await Promise.all([readKeys(), ctx.context.adapter.count({
 			model: "apikey",
 			where: [{
 				field: "referenceId",
@@ -472,19 +467,8 @@ async function listApiKeys$1(ctx, referenceId, opts, paginationOpts) {
 			total: apiKeys.length
 		};
 	}
-	const [apiKeys, total] = await Promise.all([ctx.context.adapter.findMany({
-		model: "apikey",
-		where: [{
-			field: "referenceId",
-			value: referenceId
-		}],
-		limit,
-		offset,
-		sortBy: sortBy ? {
-			field: sortBy,
-			direction: sortDirection || "asc"
-		} : void 0
-	}), ctx.context.adapter.count({
+
+	const [apiKeys, total] = await Promise.all([readKeys(), ctx.context.adapter.count({
 		model: "apikey",
 		where: [{
 			field: "referenceId",
@@ -821,6 +805,7 @@ function createApiKey({ defaultKeyGenerator, configurations, schema, deleteAllEx
 			referenceId,
 			lastRefillAt: null,
 			lastRequest: null,
+			rateLimitWindowStart: null,
 			metadata: null,
 			rateLimitMax: rateLimitMax ?? opts.rateLimit.maxRequests ?? null,
 			rateLimitTimeWindow: rateLimitTimeWindow ?? opts.rateLimit.timeWindow ?? null,
@@ -1580,7 +1565,7 @@ function updateApiKey({ configurations, schema, deleteAllExpiredApiKeys }) {
 */
 function evaluateRateLimit(apiKey, opts) {
 	const now = /* @__PURE__ */ new Date();
-	const lastRequest = apiKey.lastRequest;
+	const windowStart = apiKey.rateLimitWindowStart ?? null;
 	const rateLimitTimeWindow = apiKey.rateLimitTimeWindow;
 	const rateLimitMax = apiKey.rateLimitMax;
 	if (opts.rateLimit.enabled === false) return {
@@ -1595,12 +1580,12 @@ function evaluateRateLimit(apiKey, opts) {
 		type: "skip",
 		lastRequest: null
 	};
-	if (lastRequest === null) return {
+	if (windowStart === null) return {
 		type: "start",
 		now
 	};
-	const timeSinceLastRequest = now.getTime() - new Date(lastRequest).getTime();
-	if (timeSinceLastRequest > rateLimitTimeWindow) return {
+	const timeSinceWindowStart = now.getTime() - new Date(windowStart).getTime();
+	if (timeSinceWindowStart >= rateLimitTimeWindow) return {
 		type: "reset",
 		now,
 		windowStart: new Date(now.getTime() - rateLimitTimeWindow)
@@ -1608,7 +1593,7 @@ function evaluateRateLimit(apiKey, opts) {
 	if (apiKey.requestCount >= rateLimitMax) return {
 		type: "deny",
 		message: API_KEY_ERROR_CODES.RATE_LIMIT_EXCEEDED.message,
-		tryAgainIn: Math.ceil(rateLimitTimeWindow - timeSinceLastRequest)
+		tryAgainIn: Math.ceil(rateLimitTimeWindow - timeSinceWindowStart)
 	};
 	return {
 		type: "increment",
@@ -1811,7 +1796,7 @@ async function consumeRateLimit(ctx, apiKey, opts) {
 					value: apiKey.id
 				},
 				{
-					field: "lastRequest",
+					field: "rateLimitWindowStart",
 					operator: "gt",
 					value: decision.windowStart
 				},
@@ -1836,11 +1821,11 @@ async function consumeRateLimit(ctx, apiKey, opts) {
 		return consumeRateLimit(ctx, fresh, opts);
 	}
 	const windowGuard = decision.type === "reset" ? {
-		field: "lastRequest",
+		field: "rateLimitWindowStart",
 		operator: "lte",
 		value: decision.windowStart
 	} : {
-		field: "lastRequest",
+		field: "rateLimitWindowStart",
 		operator: "eq",
 		value: null
 	};
@@ -1853,6 +1838,7 @@ async function consumeRateLimit(ctx, apiKey, opts) {
 		increment: {},
 		set: {
 			requestCount: 1,
+			rateLimitWindowStart: decision.now,
 			lastRequest: decision.now
 		}
 	});
@@ -1933,6 +1919,7 @@ function applyRateLimitToSnapshot(apiKey, opts) {
 		case "skip": return decision.lastRequest === null ? {} : { lastRequest: decision.lastRequest };
 		case "start":
 		case "reset": return {
+			rateLimitWindowStart: decision.now,
 			lastRequest: decision.now,
 			requestCount: 1
 		};
@@ -2255,6 +2242,12 @@ const apiKeySchema = ({ defaultRateLimitMax, defaultTimeWindow }) => ({ apikey:
 		required: false,
 		input: false
 	},
+	/** Start of the current fixed rate-limit window, independent of activity. */
+	rateLimitWindowStart: {
+		type: "date",
+		required: false,
+		input: false
+	},
 	/**
 	* The date and time when the key will expire.
 	*/
diff --git a/dist/types-y22rgFHR.d.mts b/dist/types-y22rgFHR.d.mts
index 3d896fc..b38c178 100644
--- a/dist/types-y22rgFHR.d.mts
+++ b/dist/types-y22rgFHR.d.mts
@@ -153,6 +153,11 @@ declare const apiKeySchema: ({
         required: false;
         input: false;
       };
+      rateLimitWindowStart: {
+        type: "date";
+        required: false;
+        input: false;
+      };
       /**
        * The date and time when the key will expire.
        */
@@ -515,6 +520,7 @@ type ApiKey = {
    * When last request occurred
    */
   lastRequest: Date | null;
+  rateLimitWindowStart: Date | null;
   /**
    * Expiry date of a key
    */
